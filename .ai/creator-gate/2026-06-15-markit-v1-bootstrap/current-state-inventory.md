# Current State Inventory

当前 repo 初始状态：

- `.git/`
- `LICENSE`
- `docs/implementation-plan.md`
- `.DS_Store` 本地文件，已通过 `.gitignore` 排除。
- `.xmem/` 本地目录，按用户指示加入 `.gitignore`，不作为产品源。

无旧 web app、server、routes、components、DB schema。
