# WriteEngine Task

status: PASS

H tree: only bootstrap `H1 Markit` and `H2 Recent sessions`.

Exact copy: `Markit`, `Capture UI bugs from any URL`.

Do not fabricate: no long-form content, SEO copy, article body, legal text, or external facts are generated.

Decision: WriteEngine not required because visible text is below 800 characters.
